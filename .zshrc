# shell prompt

# shell aliases
alias dotfiles="/usr/bin/git --git-dir=$HOME/.dotfiles --work-tree=$HOME"

alias la="ls -a"
alias lh="ls -lh"
alias ll="ls -l"

# enable interactivity
alias cp="cp -i"
alias mv="mv -i"
alias rm="rm -i"
